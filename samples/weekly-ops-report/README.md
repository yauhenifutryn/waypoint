# weekly-ops-report

A single-page "Ops Weekly" report showing fictional team delivery and support metrics for the past week. It is maintained by the ops team and read by everyone at the Monday standup who does not want to open four dashboards. The page is regenerated from `data/metrics.json` via `node build.mjs`, so the checked-in `dist/index.html` is always reproducible.
